<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jwongp.integrador2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin_usu_edit"></activity>
        <activity android:name=".usu_reservas" />
        <activity android:name=".usu_vehiculo_pago" />
        <activity android:name=".usu_vehiculo_confirm" />
        <activity android:name=".admin_reservas" />
        <activity android:name=".admin_usuarios" />
        <activity android:name=".admin_vehiculos" />
        <activity android:name=".admin" />
        <activity android:name=".selector" />
        <activity android:name=".elegir_vehiculo" />
        <activity android:name=".about_me" />
        <activity android:name=".nueva_reserva" />
        <activity android:name=".recupera_ps" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_regusuario" />
        <activity android:name=".activity_principal" />
    </application>

</manifest>